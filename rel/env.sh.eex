#!/bin/sh

if [[ -z "$PRIVATE_ADDRESS" ]]; then
  export PRIVATE_ADDRESS=`curl http://169.254.169.254/latest/meta-data/local-ipv4`
fi

export RELEASE_DISTRIBUTION=name
export RELEASE_NODE=<%= @release.name %>@${PRIVATE_ADDRESS}
